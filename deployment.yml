---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ccit-app
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ccit-app
  template:
    metadata:
      labels:
        app: ccit-app
    spec:
      containers:
        - name: cont-1
          image: cloudintelugu/ccitimage:v1
          ports:
            - containerPort: 80
